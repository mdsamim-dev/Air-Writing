# Neon Air Writing System — Python Dependencies
# Install with: pip install -r requirements.txt

opencv-python>=4.8.0       # Webcam capture + image processing + drawing
mediapipe>=0.10.0           # Hand tracking & landmark detection
numpy>=1.24.0               # Array math for smooth filtering
